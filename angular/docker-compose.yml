version: "3"
services:
  zinlo_web:
    container_name: zinlo_web
    build:
      context: .
      args:
        APP_API_URL: "http://mac-arg.mtr-local.com"
        IGNORE_API_PREFIX: 0
    ports:
      - "8001:80"
